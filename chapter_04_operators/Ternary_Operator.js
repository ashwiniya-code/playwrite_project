let my_age = 25;
let Can_I_vote = (my_age >= 18)? "Yes, I can vote" : "No, I cannot vote";
console.log(Can_I_vote); 

let is_raining = false;
let should_I_take_umbrella = is_raining ? "Yes, take an umbrella" : "No, you don't need an umbrella";
console.log(should_I_take_umbrella);

let temperature = 30;
let weather_condition = (temperature > 25) ? "It's hot outside" : "It's cold outside";
console.log(weather_condition);

let score = 95;
let grade = (score >=90) ? "A" : (score >= 80) ? "B" : (score >= 70) ? "C" : (score >= 60) ? "D" : "F";
//console.log(`Your grade is: ${grade}`);
console.log(grade);

let is_weekend = true;
let activity = is_weekend ? "Relax and enjoy your weekend" : "Go to work";
console.log(activity);

let is_beautiful = true;
let marry = is_beautiful ? "let's say yes" : "say no";
console.log(marry);

let salary = 100;
let buy_car = (salary >=75000) ? "you can extend your limit" : (salary>=50000) ? "buy the car within range" : "Don't buy it now";
console.log(buy_car);

